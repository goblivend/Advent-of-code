def calculate(number) :
    w=0
    x=0
    y=0
    z=0
    nb = [ int(c) for c in list(str(number))]
    i = 0
    w = nb[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 12
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 1
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 12
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 1
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 15
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 16
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -8
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 5
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -4
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 9
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 15
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 14
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 2
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 14
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 15
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -13
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 5
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -3
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 11
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -7
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 7
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    x = x + 10
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 1
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -6
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    x = x * 0
    x = x + z
    x = x % 26
    z = z // 26
    x = x + -8
    w = nb[i]
    i += 1
    x = x == w
    x = x == 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    return (w, x, y, z)
